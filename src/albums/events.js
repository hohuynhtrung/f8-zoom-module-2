/**
 * events.js - Gắn event listener cho feature Albums
 * Hiện tại chưa có sự kiện (play button, v.v.)
 */

export default function initAlbumEvents() {
  // Dự phòng cho các sự kiện albums sau này
}
